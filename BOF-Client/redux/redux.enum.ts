export enum WidgetList {
  ALL = 'ALL',
  TOP = 'TOP',
}

export enum TabsList {
  LEVERAGE = 'Leverage',
  SWAP = 'Swap',
  LIMIT = 'Limit',
  CROSS_CHAIN = 'cross-chain',
}

export enum LeverageSide {
  BUY = 'BUY',
  SELL = 'SELL',
}
